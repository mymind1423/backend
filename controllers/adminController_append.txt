
import { getLiveDashboardData } from "../services/dbService.js";

export async function getLiveManagerData(req, res, next) {
  try {
    const { companyId, status } = req.query;
    const data = await getLiveDashboardData({ companyId, status });
    res.json(data);
  } catch (err) { next(err); }
}
